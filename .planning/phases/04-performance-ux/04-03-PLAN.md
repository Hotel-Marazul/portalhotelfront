---
phase: 04-performance-ux
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - frontend/src/app/reservas/page.tsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Clicar no botao Ver Detalhes em qualquer linha da tabela abre o ReservationDrawer"
    - "O drawer exibe os dados da reserva clicada (cliente, quarto, datas, hospedes, total)"
    - "O drawer pode ser fechado sem efeito colateral"
    - "Nao ha novo componente criado — ReservationDrawer.tsx existente e reutilizado"
  artifacts:
    - path: "frontend/src/app/reservas/page.tsx"
      provides: "Botao Ver Detalhes com onClick + ReservationDrawer em mode=view"
      contains: "setViewing"
  key_links:
    - from: "IconButton aria-label=ver"
      to: "ReservationDrawer"
      via: "useState viewing + onClick={() => setViewing(reservation)}"
      pattern: "setViewing"
---

<objective>
Conectar o botao "Ver Detalhes" (atualmente no-op) ao componente ReservationDrawer existente, abrindo-o em modo view com os dados da reserva selecionada.

Purpose: O botao existe na tabela de reservas mas nao tem onClick — e morto. O ReservationDrawer ja implementa completamente a visualizacao de detalhes em mode="view". A conexao e trivial: um useState + onClick + montagem do componente no JSX.

Output:
- frontend/src/app/reservas/page.tsx com estado viewing, onClick no botao e ReservationDrawer montado
</objective>

<execution_context>
@C:\Projetos\portalhotelfront\.planning\phases\04-performance-ux\04-RESEARCH.md
</execution_context>

<context>
@C:\Projetos\portalhotelfront\.planning\PROJECT.md

<interfaces>
<!-- Tipos e contratos extraidos do codebase. -->

De frontend/src/components/reservations/ReservationDrawer.tsx (interface de props):
```typescript
interface ReservationDrawerProps {
  open: boolean;
  reservation: ReservationDto | null;
  mode: "view" | "edit";
  onClose: () => void;
  onSave: (reservation: ReservationDto) => void;
  onStatusChange: (reservationId: string, status: ReservationStatus) => Promise<void>;
}
```

De frontend/src/app/reservas/page.tsx (estado atual do botao, linha 337):
```tsx
// ESTADO ATUAL — no-op
<IconButton size="small" aria-label="ver">
  <Visibility />
</IconButton>

// Estados existentes relevantes (linhas 98-101):
const [editing, setEditing] = useState<ReservationDto | null>(null);
const [deleting, setDeleting] = useState<ReservationDto | null>(null);

// Fechamento do Dialog de deleting (linha 455):
</Dialog>   ← adicionar ReservationDrawer APOS esta linha, antes de </Box>
```

NOTA: Se 04-01-PLAN.md ja foi executado antes desta plan, `loadReservations` agora tem assinatura
`useCallback(async () => { ... }, [page, rowsPerPage])`. O callback onStatusChange deve chamar
`void loadReservations()` — que ja funciona sem argumentos (usa os estados do closure).
Se 04-01 ainda nao foi executado, `loadReservations` ainda e `useCallback(async () => {...}, [])`.
Em ambos os casos, `void loadReservations()` funciona.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Conectar botao Ver Detalhes ao ReservationDrawer</name>
  <files>frontend/src/app/reservas/page.tsx</files>

  <read_first>
    - frontend/src/app/reservas/page.tsx (completo — identificar: bloco de imports linhas 1-35 para saber o que ja esta importado; estados existentes linhas 89-101; o botao "ver" exato nas linhas ~337-339; o ultimo Dialog de deleting que fecha em ~linha 455; o fechamento </Box> final)
    - frontend/src/components/reservations/ReservationDrawer.tsx (linhas 1-50 — confirmar o nome do export default e as props exatas)
  </read_first>

  <action>
### Mudancas em frontend/src/app/reservas/page.tsx

#### 1. Import do ReservationDrawer
Verificar se ReservationDrawer ja esta importado. Se nao estiver, adicionar ao bloco de imports:
```typescript
import ReservationDrawer from "../../components/reservations/ReservationDrawer";
```
Usar o caminho relativo a partir de src/app/reservas/page.tsx. Verificar o export do componente (default ou named) lendo as primeiras linhas do arquivo.

#### 2. Estado viewing
Adicionar apos os estados de editing/deleting (linha ~101):
```typescript
const [viewing, setViewing] = useState<ReservationDto | null>(null);
```
O tipo ReservationDto ja deve estar importado. Se nao estiver, confirmar o import existente.

#### 3. Atualizar botao "Ver Detalhes"
Substituir o IconButton no-op (linhas ~337-339):
```tsx
// ANTES:
<IconButton size="small" aria-label="ver">
  <Visibility />
</IconButton>

// DEPOIS:
<IconButton
  size="small"
  aria-label="ver"
  onClick={() => setViewing(reservation)}
>
  <Visibility />
</IconButton>
```
A variavel `reservation` e o item do map de `filteredSorted` (ou `allReservations`) que ja existe no escopo do render.

#### 4. Montar ReservationDrawer no JSX
Adicionar APOS o fechamento do Dialog de confirmacao de exclusao (apos </Dialog> da linha ~455), antes do fechamento do </Box> principal:
```tsx
<ReservationDrawer
  open={!!viewing}
  reservation={viewing}
  mode="view"
  onClose={() => setViewing(null)}
  onSave={(updated) => {
    setAllReservations(prev => prev.map(r => r.id === updated.id ? updated : r));
    setViewing(null);
  }}
  onStatusChange={async (id, status) => {
    await apiClient.put(`/api/Reservations/${id}`, { ...viewing!, status });
    void loadReservations();
  }}
/>
```

IMPORTANTE: Nao criar nenhum novo componente. Nao alterar ReservationDrawer.tsx. Apenas conectar no page.tsx.

IMPORTANTE: O `onSave` recebe `ReservationDto` — verificar que o tipo corresponde ao que ReservationDrawer.tsx passa no callback. Caso haja discrepancia de tipo, adicionar cast ou adaptar.
  </action>

  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "setViewing" frontend/src/app/reservas/page.tsx retorna >= 2 (declaracao do estado + uso no onClick + uso no onClose = pelo menos 2)
    - grep -c "ReservationDrawer" frontend/src/app/reservas/page.tsx retorna >= 2 (import + uso no JSX)
    - grep -c 'mode="view"' frontend/src/app/reservas/page.tsx retorna >= 1
    - grep -c 'aria-label="ver"' frontend/src/app/reservas/page.tsx retorna >= 1
    - grep -c "onClick.*setViewing" frontend/src/app/reservas/page.tsx retorna >= 1
    - npm run build em frontend/ termina sem erros TypeScript
  </acceptance_criteria>

  <done>
    Clicar em qualquer botao "Ver Detalhes" na tabela de reservas abre o ReservationDrawer com os dados da reserva. O drawer pode ser fechado. Build sem erros TypeScript.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descricao |
|----------|-----------|
| Browser → PUT /Reservations/:id | onStatusChange faz PUT — dados vem do estado local (viewing), nao de input do usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | PUT /Reservations/:id via onStatusChange | accept | Dados vem de ReservationDto do estado autenticado — nao e input livre do usuario; authMiddleware protege a rota |
| T-04-10 | Information Disclosure | Drawer exibe dados da reserva (cliente, CPF) | accept | Feature intencional do sistema interno — sem exposicao externa; autenticacao ja exigida para acessar /reservas |
</threat_model>

<verification>
Apos execucao:

1. Frontend compila sem erros: `cd frontend && npm run build`
2. Verificacao manual: acessar http://localhost:3000/reservas, clicar no icone de olho em qualquer linha — drawer deve abrir mostrando detalhes da reserva
3. Fechar o drawer clicando em X ou fora — deve fechar sem erro
4. Verificar que os botoes de editar e excluir continuam funcionando normalmente (sem regressao)
</verification>

<success_criteria>
- Botao "Ver Detalhes" tem onClick que chama setViewing(reservation)
- ReservationDrawer esta montado no JSX com mode="view", open={!!viewing}, reservation={viewing}
- Fechar o drawer chama setViewing(null)
- npm run build em frontend/ sem erros TypeScript
- Nenhum novo componente criado — ReservationDrawer.tsx nao foi modificado
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/04-performance-ux/04-03-SUMMARY.md` seguindo o template em `@C:\Projetos\portalhotelfront\.planning\phases\01-data-integrity\01-01-SUMMARY.md` como referencia de formato.
</output>
