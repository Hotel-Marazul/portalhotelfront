---
phase: 04-performance-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/reservations/reservations.routes.ts
  - frontend/src/app/reservas/page.tsx
  - frontend/src/app/cliente/page.tsx
autonomous: true
requirements:
  - PERF-01

must_haves:
  truths:
    - "GET /Reservations?page=2&limit=20 retorna exatamente 20 itens com campo total"
    - "O frontend /reservas exibe o total correto de registros no TablePagination"
    - "A paginacao em /reservas dispara uma nova request ao backend (nao filtra em memoria)"
    - "A pagina /cliente nao quebra ao carregar — useCachedFetch de reservas adaptado para envelope"
    - "limit=999999 e truncado para 100 pelo backend"
  artifacts:
    - path: "backend/src/modules/reservations/reservations.routes.ts"
      provides: "GET /Reservations com LIMIT/OFFSET e resposta envelope"
      contains: "Promise.all"
    - path: "frontend/src/app/reservas/page.tsx"
      provides: "Paginacao server-side com total do servidor"
      contains: "ReservationsResponse"
    - path: "frontend/src/app/cliente/page.tsx"
      provides: "useCachedFetch adaptado para envelope de reservas"
      contains: "reservationsData?.items"
  key_links:
    - from: "frontend/src/app/reservas/page.tsx"
      to: "/api/Reservations"
      via: "apiClient.get com params { page: page+1, limit }"
      pattern: "page.*\\+.*1"
    - from: "backend/src/modules/reservations/reservations.routes.ts"
      to: "PostgreSQL"
      via: "Promise.all com query COUNT e query LIMIT/OFFSET"
      pattern: "Promise\\.all"
---

<objective>
Implementar paginacao server-side no endpoint GET /Reservations e migrar o frontend de paginacao client-side para server-side.

Purpose: O endpoint atual busca todas as reservas sem LIMIT, o que degrada com o volume. O frontend pagina em memoria sobre o array completo. Apos esta plan, o banco retorna apenas a pagina solicitada.

Output:
- Backend: GET /Reservations aceita ?page e ?limit, retorna envelope { items, total, page, pageSize }
- Frontend /reservas: loadReservations envia page/limit, TablePagination usa count=total do servidor
- Frontend /cliente: useCachedFetch<Reservation[]> atualizado para ReservationsResponse e acessa .items
</objective>

<execution_context>
@C:\Projetos\portalhotelfront\.planning\phases\04-performance-ux\04-RESEARCH.md
@C:\Projetos\portalhotelfront\.planning\phases\04-performance-ux\04-PATTERNS.md
</execution_context>

<context>
@C:\Projetos\portalhotelfront\.planning\PROJECT.md
@C:\Projetos\portalhotelfront\.planning\ROADMAP.md

<interfaces>
<!-- Tipos e contratos que o executor precisa. Extraidos do codebase. -->

De frontend/src/types/reservations.ts:
```typescript
export interface ReservationsResponse {
  items: ReservationDto[];
  total: number;
  page: number;
  pageSize: number;
}

export interface ReservationDto {
  id: string;
  roomId: string;
  clientId: string;
  checkInDate: string;
  checkOutDate: string;
  status: ReservationStatus;
  totalPrice?: number | null;
  room?: { id: string; name?: string; number?: number; type?: string; dailyPrice?: number | null; };
  client?: { id: string; name: string; fullName?: string; cpf: string; };
  guests: ReservationGuestDto[];
}
```

De backend/src/modules/reservations/reservations.routes.ts (estado atual, linhas 244-250):
```typescript
// ESTADO ATUAL — a ser substituido
reservationsRouter.get(
  "/Reservations",
  asyncHandler(async (_req, res) => {
    const reservations = await getReservationsByIds();
    res.json(reservations);  // retorna array simples
  })
);
```

Funcao auxiliar existente no mesmo arquivo (linha ~134):
```typescript
// reservationRowsToDto ja existe — nao recriar
function reservationRowsToDto(reservationRows: ReservationRow[], guestRows: GuestRow[]): ReservationDto[]
```

De frontend/src/app/cliente/page.tsx (estado atual, linhas 60-68):
```typescript
// ESTADO ATUAL — a ser adaptado
const { data: reservationsData } = useCachedFetch<Reservation[]>('/api/reservations', {
  cacheKey: 'reservations',
  expiresIn: 5 * 60 * 1000
});
// Linha 85: const reservations: Reservation[] = reservationsData || [];
```

De frontend/src/app/reservas/page.tsx (estado atual):
```typescript
// Linha 107 — a ser migrado:
const res = await apiClient.get<ReservationDto[]>("/api/Reservations");
setAllReservations(Array.isArray(res.data) ? res.data : []);

// Linha 375 — a ser atualizado:
count={filteredSorted.length}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Backend — GET /Reservations com LIMIT/OFFSET e envelope</name>
  <files>backend/src/modules/reservations/reservations.routes.ts</files>

  <read_first>
    - backend/src/modules/reservations/reservations.routes.ts (linhas 244-250 — handler atual a substituir; linhas 134-186 — funcao reservationRowsToDto existente; linhas 192-242 — funcao getReservationsByIds para referencia de tipos GuestRow/ReservationRow)
  </read_first>

  <behavior>
    - Test 1: GET /Reservations sem params retorna { items: [...], total: N, page: 1, pageSize: 20 }
    - Test 2: GET /Reservations?page=2&limit=5 retorna exatamente 5 itens (se existirem) e total correto
    - Test 3: GET /Reservations?limit=999 e truncado para maximo 100 itens
    - Test 4: GET /Reservations?page=-1 e tratado como page=1 (sem offset negativo)
    - Test 5: GET /Reservations em banco vazio retorna { items: [], total: 0, page: 1, pageSize: 20 }
  </behavior>

  <action>
Substituir o handler GET /Reservations (linhas 244-250) pelo handler paginado abaixo. NAO alterar nenhum outro handler no arquivo.

Regras de sanitizacao (seguranca contra Tampering — T-04-01):
- page: Math.max(1, parseInt(req.query.page as string ?? "1", 10) || 1)
- limit: Math.min(100, Math.max(1, parseInt(req.query.limit as string ?? "20", 10) || 20))
- offset: (page - 1) * limit
- NUNCA interpolar page/limit na string SQL — usar $1/$2 parametrizados

Usar Promise.all para COUNT e dados em paralelo (NAO sequencial — causa N+1):

```typescript
reservationsRouter.get(
  "/Reservations",
  asyncHandler(async (req, res) => {
    const page   = Math.max(1, parseInt(req.query.page  as string ?? "1",  10) || 1);
    const limit  = Math.min(100, Math.max(1, parseInt(req.query.limit as string ?? "20", 10) || 20));
    const offset = (page - 1) * limit;

    const [reservationRows, countResult] = await Promise.all([
      query<ReservationRow>(
        `SELECT r.id, r.room_id, r.client_id,
                r.check_in_date::text, r.check_out_date::text,
                r.status, r.total_price::text,
                rm.number AS room_number, rm.type AS room_type,
                rm.daily_price::text AS room_daily_price,
                cl.full_name AS client_full_name, cl.cpf AS client_cpf
         FROM reservations r
         LEFT JOIN rooms rm ON rm.id = r.room_id
         LEFT JOIN clients cl ON cl.id = r.client_id
         ORDER BY r.check_in_date DESC, r.created_at DESC
         LIMIT $1 OFFSET $2`,
        [limit, offset]
      ),
      query<{ total: number }>(
        `SELECT COUNT(*)::int AS total FROM reservations`
      )
    ]);

    const total = countResult[0]?.total ?? 0;

    if (reservationRows.length === 0) {
      return res.json({ items: [], total, page, pageSize: limit });
    }

    const ids = reservationRows.map(r => r.id);
    const guestRows = await query<GuestRow>(
      `SELECT g.id, g.reservation_id, g.name, g.age, g.pricing_rule_id,
              pr.id AS rule_id, pr.name AS rule_name,
              pr.description AS rule_description, pr.price::text AS rule_price
       FROM reservation_guests g
       LEFT JOIN pricing_rules pr ON pr.id = g.pricing_rule_id
       WHERE g.reservation_id = ANY($1::uuid[])
       ORDER BY g.created_at ASC`,
      [ids]
    );

    const items = reservationRowsToDto(reservationRows, guestRows);
    res.json({ items, total, page, pageSize: limit });
  })
);
```

O tipo `req` substitui `_req` — mudar o parametro de `_req` para `req` no handler.
  </action>

  <verify>
    <automated>cd backend && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "Promise.all" backend/src/modules/reservations/reservations.routes.ts retorna >= 1
    - grep -c "LIMIT \$1 OFFSET \$2" backend/src/modules/reservations/reservations.routes.ts retorna >= 1
    - grep -c "COUNT(\*)::int AS total" backend/src/modules/reservations/reservations.routes.ts retorna >= 1
    - grep -c "pageSize" backend/src/modules/reservations/reservations.routes.ts retorna >= 1
    - npm run build em backend/ termina sem erros TypeScript
  </acceptance_criteria>

  <done>GET /Reservations retorna { items, total, page, pageSize } com LIMIT/OFFSET parametrizado e COUNT em Promise.all paralelo. Build TypeScript sem erros.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Frontend /reservas — paginacao server-side + fix /cliente useCachedFetch</name>
  <files>
    frontend/src/app/reservas/page.tsx
    frontend/src/app/cliente/page.tsx
  </files>

  <read_first>
    - frontend/src/app/reservas/page.tsx (completo — entender estados existentes: page, rowsPerPage, allReservations, filters; funcao loadReservations linhas 103-119; TablePagination linha 375; botao "editar" linha 340-345 como referencia de padroa onClick)
    - frontend/src/app/cliente/page.tsx (linhas 60-85 — useCachedFetch de reservas e como e usado; linha 85 const reservations)
    - frontend/src/types/reservations.ts (ReservationsResponse ja definido — importar em vez de redefinir)
  </read_first>

  <action>
### Arquivo: frontend/src/app/reservas/page.tsx

1. Adicionar import de ReservationsResponse (se ainda nao importado):
   ```typescript
   import { ReservationDto, ReservationsFilters, ReservationsResponse } from "../../types/reservations";
   ```
   (verificar se ReservationsResponse ja esta no import existente — se sim, nao duplicar)

2. Adicionar estado `total` apos os estados existentes (linha ~101):
   ```typescript
   const [total, setTotal] = useState(0);
   ```

3. Substituir a funcao loadReservations (linhas 103-119) por versao que envia page/limit:
   ```typescript
   const loadReservations = useCallback(async () => {
     setLoading(true);
     setError(null);
     try {
       const res = await apiClient.get<ReservationsResponse>("/api/Reservations", {
         params: { page: page + 1, limit: rowsPerPage }  // MUI base-0 → backend base-1
       });
       setAllReservations(res.data.items ?? []);
       setTotal(res.data.total ?? 0);
     } catch (e: unknown) {
       if (isAxiosError(e)) {
         setError(e.response?.data?.message ?? "Erro ao carregar reservas");
       } else {
         setError(e instanceof Error ? e.message : "Erro ao carregar reservas");
       }
       setAllReservations([]);
     } finally {
       setLoading(false);
     }
   }, [page, rowsPerPage]);
   ```
   ATENCAO: A dependencia do useCallback agora inclui `page` e `rowsPerPage`. O useEffect existente (`void loadReservations()`) ja reage via dep-array de loadReservations — manter.

4. Substituir `count={filteredSorted.length}` no TablePagination (linha ~375) por:
   ```typescript
   count={total}
   ```

5. NAO remover o filteredSorted — ele ainda e usado para filtros locais (status, search, cpf) sobre a pagina atual. Os filtros locais atuam sobre `allReservations` (que agora e apenas a pagina). Manter o useMemo de filteredSorted intacto.

6. Manter os dois `setPage(0)` existentes (em onFiltersChange e onClear) — eles ja existem e estao corretos.

### Arquivo: frontend/src/app/cliente/page.tsx

O `useCachedFetch<Reservation[]>('/api/reservations', ...)` vai receber um objeto envelope apos PERF-01. Adaptar:

1. Mudar o tipo do hook de `Reservation[]` para o envelope. Adicionar interface local ou importar ReservationsResponse do types (preferir importar):
   ```typescript
   import { ReservationsResponse } from '../../types/reservations';
   ```

2. Mudar a chamada useCachedFetch:
   ```typescript
   const {
     data: reservationsData,
     loading: loadingReservations,
     error: errorReservations,
     refetch: refetchReservations
   } = useCachedFetch<ReservationsResponse>('/api/reservations', {
     cacheKey: 'reservations',
     expiresIn: 5 * 60 * 1000
   });
   ```
   NOTA: A URL '/api/reservations' (lowercase) e diferente de '/api/Reservations' (PascalCase) do backend. Verificar se o backend responde em ambas as rotas. Se '/api/reservations' nao funcionar, mudar para '/api/Reservations'. Priorizar o que ja estava funcionando antes.

3. Mudar a linha de uso do dado (linha ~85):
   ```typescript
   // ANTES:
   const reservations: Reservation[] = reservationsData || [];

   // DEPOIS:
   const reservations = reservationsData?.items ?? [];
   ```
   O tipo de `reservations` sera inferido como `ReservationDto[]` — verificar se `ReservationTimeline` aceita esse tipo ou se precisa de cast.

4. Manter o restante do arquivo intacto.
  </action>

  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "ReservationsResponse" frontend/src/app/reservas/page.tsx retorna >= 1
    - grep -c "res.data.items" frontend/src/app/reservas/page.tsx retorna >= 1
    - grep -c "res.data.total" frontend/src/app/reservas/page.tsx retorna >= 1
    - grep -c "count={total}" frontend/src/app/reservas/page.tsx retorna >= 1
    - grep -c "page: page + 1" frontend/src/app/reservas/page.tsx retorna >= 1
    - grep -c "reservationsData?.items" frontend/src/app/cliente/page.tsx retorna >= 1
    - npm run build em frontend/ termina sem erros TypeScript
  </acceptance_criteria>

  <done>
    A pagina /reservas envia page/limit ao backend e exibe total do servidor no TablePagination. A pagina /cliente acessa .items do envelope de reservas sem erros TypeScript. Build sem erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descricao |
|----------|-----------|
| Cliente HTTP → GET /Reservations | Parametros page e limit chegam como strings nao confiadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | GET /Reservations — query param limit | mitigate | Math.min(100, ...) — teto de 100 registros; nunca interpolar na SQL |
| T-04-02 | Tampering | GET /Reservations — query param page | mitigate | Math.max(1, ...) — impede offset negativo |
| T-04-03 | Tampering | SQL injection via page/limit | mitigate | Valores passados como $1/$2 parametrizados — nunca concatenados na string SQL |
| T-04-04 | Information Disclosure | Total count exposto | accept | Apenas contagem total — sem dado sensivel; authMiddleware ja protege a rota |
</threat_model>

<verification>
Apos execucao:

1. Backend compilado sem erros: `cd backend && npm run build`
2. Resposta envelope: iniciar backend e executar `curl -s "http://localhost:5000/api/Reservations?page=1&limit=5" | python3 -m json.tool | grep -E "total|page|pageSize|items"`
3. Seguranca — limite maximo: `curl -s "http://localhost:5000/api/Reservations?limit=9999"` deve retornar no maximo 100 itens em items
4. Frontend compila sem erros: `cd frontend && npm run build`
5. Acessar /reservas — TablePagination deve mostrar o total correto (nao mais o length do array filtrado)
</verification>

<success_criteria>
- GET /Reservations retorna envelope { items, total, page, pageSize } com dados apenas da pagina
- limit=999999 e truncado para max 100 pelo backend
- Frontend /reservas TablePagination usa count=total (vindo do servidor)
- Frontend /cliente nao quebra — reservationsData?.items funciona
- npm run build em backend/ e frontend/ sem erros TypeScript
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/04-performance-ux/04-01-SUMMARY.md` seguindo o template em `@C:\Projetos\portalhotelfront\.planning\phases\01-data-integrity\01-01-SUMMARY.md` como referencia de formato.
</output>
